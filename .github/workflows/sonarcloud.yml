name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read

jobs:
  sonarcloud:
    name: 🔎 SonarCloud
    if: github.repository == 'utilForever/roguekit' && (github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository)
    runs-on: ubuntu-latest
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout sources
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Install stable toolchain
        uses: dtolnay/rust-toolchain@stable
      - name: Install cargo-tarpaulin
        if: ${{ env.SONAR_TOKEN != '' }}
        run: cargo install cargo-tarpaulin
      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@v2
      - name: Build project
        if: ${{ env.SONAR_TOKEN != '' }}
        run: cargo build --workspace
      - name: Generate coverage report
        if: ${{ env.SONAR_TOKEN != '' }}
        run: cargo tarpaulin --workspace --out Lcov --output-dir coverage
      - name: Skip scan when SONAR_TOKEN is missing
        if: ${{ env.SONAR_TOKEN == '' }}
        run: echo "SONAR_TOKEN is not configured. Skipping SonarCloud scan."
      - name: Run SonarCloud scan
        if: ${{ env.SONAR_TOKEN != '' }}
        uses: SonarSource/sonarqube-scan-action@v8
